# Publisher configuration is separate from controller and worker credentials.
# The file referenced here must be mode 0600 and remain in the state root.
AGENTD_PUBLISHER_CONFIG=/home/bened/.local/state/agentd/publisher.json
